# ==== Akka ==== #
snippet actor
	object ${1:Name} {
		def props(${2:args}) = {
			Props(new $1(${3:args}))
		}
	}


	/**
	 * TODO: fill me
	 */
	class $1($2) extends Actor with ${4:Logging} {
		def receive = {
			case $5 => $6
		}
	}

# ====

snippet schedule
	context.system.scheduler.schedule(
		${1:0}.seconds, ${2:10}.seconds,
		${3:actor}, ${4:Timeout})
